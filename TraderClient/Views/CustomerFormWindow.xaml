<Window x:Class="TraderClient.Views.CustomerFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TraderClient.Views"
        mc:Ignorable="d"
        Title="CustomerFormWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 5">
            <TextBlock Text="Id:" Width="80"/>
            <TextBox Text="{Binding Id}" Width="250"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 5" Grid.Row="1">
            <TextBlock Text="Name:" Width="80"/>
            <TextBox Text="{Binding Name}" Width="250"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 5" Grid.Row="2">
            <TextBlock Text="City:" Width="80"/>
            <TextBox Text="{Binding City}" Width="250"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Left" Margin="0 10">
            <Button Content="OK" Width="80" Margin="5" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
