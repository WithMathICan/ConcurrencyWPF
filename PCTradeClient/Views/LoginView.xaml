<Window x:Class="PCTradeClient.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCTradeClient.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel Width="300">
            <TextBlock Text="Username"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height ="25"/>
            <TextBlock Text="Password" Margin="0,10,0,0" />
            <PasswordBox x:Name="PasswordBox" Height ="25"/>
            <Button Content="Submit" Command="{Binding LoginCommand}" Margin="0,10,0,0" Height ="25"/>
        </StackPanel>
    </Grid>
</Window>
